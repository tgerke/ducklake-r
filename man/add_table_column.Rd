% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema_evolution.R
\name{add_table_column}
\alias{add_table_column}
\title{Add a column to a DuckLake table}
\usage{
add_table_column(table_name, column_name, type, default = NULL)
}
\arguments{
\item{table_name}{The table to change.}

\item{column_name}{Name of the new column.}

\item{type}{SQL type for the new column, e.g. \code{"INTEGER"},
\code{"DECIMAL(10,2)"}, or \code{"TIMESTAMP WITH TIME ZONE"}.}

\item{default}{Optional default value (an R scalar: character, numeric,
logical, Date, or POSIXct). In DuckLake the default applies to
existing rows as well as future inserts, so the new column appears
filled everywhere. Without a default, the column reads \code{NA} for
existing rows.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Adds a column in place with \verb{ALTER TABLE ... ADD COLUMN}. This is a
metadata-only change: no data files are rewritten, history is preserved,
and earlier snapshots still show the old schema. Compare
\code{\link[=replace_table]{replace_table()}}, which collects the table into R and rewrites it.
}
\examples{
\dontrun{
add_table_column("adsl", "AGECAT", "VARCHAR")
add_table_column("sales", "discount", "DECIMAL(5,2)", default = 0)
}
}
\seealso{
\code{\link[=drop_table_column]{drop_table_column()}}, \code{\link[=rename_table_column]{rename_table_column()}},
\code{\link[=set_column_type]{set_column_type()}}, \code{\link[=rename_ducklake_table]{rename_ducklake_table()}}

Other schema evolution:
\code{\link[=drop_table_column]{drop_table_column()}},
\code{\link[=rename_ducklake_table]{rename_ducklake_table()}},
\code{\link[=rename_table_column]{rename_table_column()}},
\code{\link[=set_column_type]{set_column_type()}}
}
\concept{schema evolution}
