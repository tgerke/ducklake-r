% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quack.R
\name{attach_quack}
\alias{attach_quack}
\title{Connect to a remote Quack server}
\usage{
attach_quack(quack_name, uri, token = NULL, disable_ssl = FALSE)
}
\arguments{
\item{quack_name}{Name for the attached remote catalog, used as the database
alias in DuckDB.}

\item{uri}{Address of the Quack server, for example \code{"quack:localhost"} or
\code{"quack:data.example.org:9494"}. A bare host such as \code{"localhost"} is
prefixed with \verb{quack:} automatically. The default port is 9494.}

\item{token}{Authentication token expected by the server. If \code{NULL}, the token
is taken from a Quack secret (see \verb{CREATE SECRET}) if one exists.}

\item{disable_ssl}{Connect over plain HTTP instead of HTTPS (default \code{FALSE}).
Only appropriate on a trusted network.}
}
\description{
Attaches a remote Quack server as a catalog in the current session. Tables in
the server's default database are then reachable as \code{quack_name.table_name}
and can be queried with \code{\link[=get_ducklake_table]{get_ducklake_table()}} or \code{dplyr::tbl()}.
}
\details{
A DuckLake served over Quack lives in its own catalog on the server rather
than in the default database, so its tables are not exposed under
\code{quack_name}. Query a served DuckLake with \code{\link[=quack_query]{quack_query()}}, naming the lake's
catalog, for example \code{quack_query(uri, "SELECT * FROM trial.adsl")}.
}
\examples{
\dontrun{
# A remote DuckDB database, queried through the attached catalog
attach_quack("warehouse", "quack:data.example.org", token = "super_secret")

get_ducklake_table("warehouse.sales") |>
  dplyr::filter(region == "EMEA") |>
  dplyr::collect()

detach_quack("warehouse")
}
}
\seealso{
\code{\link[=detach_quack]{detach_quack()}}, \code{\link[=quack_query]{quack_query()}}, \code{\link[=quack_serve]{quack_serve()}}

Other quack:
\code{\link[=detach_quack]{detach_quack()}},
\code{\link[=install_quack]{install_quack()}},
\code{\link[=quack_query]{quack_query()}},
\code{\link[=quack_serve]{quack_serve()}},
\code{\link[=quack_stop]{quack_stop()}}
}
\concept{quack}
