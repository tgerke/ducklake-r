% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maintenance.R
\name{cleanup_old_files}
\alias{cleanup_old_files}
\title{Delete files scheduled for removal}
\usage{
cleanup_old_files(
  ducklake_name = NULL,
  older_than = NULL,
  cleanup_all = FALSE,
  dry_run = FALSE
)
}
\arguments{
\item{ducklake_name}{Name of the attached DuckLake catalog. If \code{NULL}, the
current database is used.}

\item{older_than}{Only delete files scheduled for deletion before this
timestamp (POSIXct, converted to UTC, or character already in UTC). One
of \code{older_than} or \code{cleanup_all} is required.}

\item{cleanup_all}{If \code{TRUE}, delete all scheduled files regardless of
when they were scheduled.}

\item{dry_run}{If \code{TRUE}, only lists the files that would be deleted.}
}
\value{
A data frame listing the deleted (or deletable) files.
}
\description{
Physically deletes data files that are no longer referenced by any
snapshot -- typically files orphaned by \code{\link[=expire_snapshots]{expire_snapshots()}} or replaced
by \code{\link[=merge_adjacent_files]{merge_adjacent_files()}}.
}
\details{
As an alternative to calling this manually, a retention policy can be set
once on the catalog with
\code{DBI::dbExecute(get_ducklake_connection(), "CALL my_lake.set_option('delete_older_than', '1 week')")},
after which DuckLake cleans up eligible files automatically.
}
\examples{
\dontrun{
# Preview, then delete everything that is scheduled
cleanup_old_files(dry_run = TRUE, cleanup_all = TRUE)
cleanup_old_files(cleanup_all = TRUE)

# Only delete files scheduled more than a week ago
cleanup_old_files(older_than = Sys.time() - 7 * 24 * 60 * 60)
}
}
\seealso{
\code{\link[=expire_snapshots]{expire_snapshots()}}, \code{\link[=delete_orphaned_files]{delete_orphaned_files()}},
\code{\link[=checkpoint_ducklake]{checkpoint_ducklake()}}

Other maintenance:
\code{\link[=backup_ducklake]{backup_ducklake()}},
\code{\link[=checkpoint_ducklake]{checkpoint_ducklake()}},
\code{\link[=delete_orphaned_files]{delete_orphaned_files()}},
\code{\link[=expire_snapshots]{expire_snapshots()}},
\code{\link[=flush_inlined_data]{flush_inlined_data()}},
\code{\link[=get_table_info]{get_table_info()}},
\code{\link[=merge_adjacent_files]{merge_adjacent_files()}},
\code{\link[=plot_table_files]{plot_table_files()}},
\code{\link[=rewrite_data_files]{rewrite_data_files()}}
}
\concept{maintenance}
