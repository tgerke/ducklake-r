% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{create_check}
\alias{create_check}
\title{Create a data check from a rule}
\usage{
create_check(
  .data,
  check_name,
  rule,
  label,
  listing = dplyr::everything(),
  schema_name = NULL,
  replace = TRUE
)
}
\arguments{
\item{.data}{A lazy table (a dplyr pipeline built on
\code{\link[=get_ducklake_table]{get_ducklake_table()}}) holding the rows to check. Read the table by its
schema-qualified name, \code{get_ducklake_table("main.cars")}, so the check
binds whichever database is current.}

\item{check_name}{The rule's id, which becomes the view's name and the
\code{check} column of \code{\link[=run_checks]{run_checks()}}.}

\item{rule}{The rule, as an expression on the columns of \code{.data} that is
\code{TRUE} for a row in good standing, such as \code{cyl \%in\% c(4, 6, 8)}.}

\item{label}{One sentence stating the rule, stored as the view's comment.}

\item{listing}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> The columns
the check returns for a failing row: what someone needs to find the row
and fix it. All columns by default.}

\item{schema_name}{The schema that holds the checks. Defaults to
\code{"checks"}, or to the schema in \code{check_name} when that is qualified
(\code{"qc.cyl_known"}). It is created if it does not exist.}

\item{replace}{Replace an existing check of the same name (default TRUE).}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
\strong{Experimental.} Stores a rule as a check: a view, in a schema set aside
for checks, that returns the rows breaking the rule, with the rule's label
as its comment. State the rule the way you would say it, as the condition
every row should meet. \code{create_check()} keeps the rows where it is false,
so the code reads like the label and a rule such as "dose is not 0" is
written \code{dose != 0}, with no double negative. \code{\link[=run_checks]{run_checks()}} then counts
the failing rows of every check in the schema.
}
\details{
A row where the rule evaluates to \code{NA} passes, as it does under a SQL
\code{CHECK} constraint: \code{NOT (rule)} is not true for it. When a missing value
should fail, say so in the rule (\code{!is.na(dose) & dose != 0}) or give it a
check of its own.

The schema, the view, and its label are written in one transaction, so a
new or revised check is one snapshot. Inside \code{\link[=with_transaction]{with_transaction()}} they
join the open transaction, which is how a change of rules gets an author
and a commit message.

A check is an ordinary view, and \code{create_check()} is a convenience for
the common case of one rule about each row. A rule that is easier to
state as its failure (models that appear twice, visits without a
subject) is a pipeline that returns those rows, stored with
\code{\link[=create_view]{create_view()}} in the same schema and labelled with
\code{\link[=set_table_comment]{set_table_comment()}}. Both forms select the same rows, since
three-valued logic treats \code{NOT (dose != 0)} and \code{dose = 0} alike, so
choose the one that reads better.
}
\examples{
\dontshow{if (ducklake_extension_available()) withAutoprint(\{ # examplesIf}
lake_dir <- tempfile("create_check_lake_")
dir.create(lake_dir)
attach_ducklake("create_check_lake", lake_path = lake_dir)
create_table(
  data.frame(model = rownames(mtcars), mtcars, row.names = NULL),
  "cars"
)

get_ducklake_table("main.cars") |>
  create_check(
    "cyl_known", cyl \%in\% c(4, 6, 8),
    label = "cyl is 4, 6, or 8",
    listing = c(model, cyl)
  )

# A rule that forbids a value is stated as it is said
get_ducklake_table("main.cars") |>
  create_check("hp_not_zero", hp != 0, label = "hp is not 0")

run_checks()

detach_ducklake("create_check_lake", shutdown = TRUE)
unlink(lake_dir, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=run_checks]{run_checks()}} to run the checks, \code{\link[=create_view]{create_view()}} for a check
written as its failure, and \code{vignette("data-checks")}.
}
