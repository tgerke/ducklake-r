% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schemas.R
\name{create_schema}
\alias{create_schema}
\title{Create a schema in a DuckLake}
\usage{
create_schema(schema_name, if_not_exists = TRUE, ducklake_name = NULL)
}
\arguments{
\item{schema_name}{Name of the schema.}

\item{if_not_exists}{When the schema already exists, say so and do
nothing (default \code{TRUE}) rather than error.}

\item{ducklake_name}{Optional name of the attached DuckLake catalog. If
\code{NULL}, the current database is used.}
}
\value{
Invisibly, \code{NULL}.
}
\description{
Schemas group the tables of a lake, and are the natural home for
medallion layers (\code{bronze}, \code{silver}, \code{gold}) or per-study areas. Every
function that takes a table name accepts \code{"schema.table"}, and
\code{\link[=list_ducklake_tables]{list_ducklake_tables()}} shows each table's schema. DuckLake writes a
schema's data files under a directory of their own, which is what
path-based access control on object storage keys on.
}
\examples{
\dontshow{if (ducklake_extension_available()) withAutoprint(\{ # examplesIf}
lake_dir <- tempfile("schema_lake_")
dir.create(lake_dir)
attach_ducklake("schema_lake", lake_path = lake_dir)

# One schema per medallion layer
create_schema("bronze")
create_schema("silver")

create_table(mtcars, "bronze.cars")
get_ducklake_table("bronze.cars") |>
  dplyr::filter(cyl == 4) |>
  create_table("silver.cars")

list_ducklake_tables()

detach_ducklake("schema_lake", shutdown = TRUE)
unlink(lake_dir, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=drop_schema]{drop_schema()}}, \code{\link[=list_ducklake_tables]{list_ducklake_tables()}}

Other table operations:
\code{\link[=add_data_files]{add_data_files()}},
\code{\link[=create_table]{create_table()}},
\code{\link[=create_view]{create_view()}},
\code{\link[=drop_schema]{drop_schema()}},
\code{\link[=drop_view]{drop_view()}},
\code{\link[=ducklake_exec]{ducklake_exec()}},
\code{\link[=get_ducklake_table]{get_ducklake_table()}},
\code{\link[=get_metadata_table]{get_metadata_table()}},
\code{\link[=list_ducklake_tables]{list_ducklake_tables()}},
\code{\link[=replace_table]{replace_table()}},
\code{\link[=show_ducklake_query]{show_ducklake_query()}}
}
\concept{table operations}
