% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/views.R
\name{create_view}
\alias{create_view}
\title{Create a DuckLake view from a dplyr pipeline}
\usage{
create_view(.data, view_name, replace = TRUE)
}
\arguments{
\item{.data}{A lazy table (a dplyr pipeline built on
\code{\link[=get_ducklake_table]{get_ducklake_table()}}). Not a data frame: a view stores a query, not
data -- use \code{\link[=create_table]{create_table()}} to store data.}

\item{view_name}{Name for the view.}

\item{replace}{Replace an existing view of the same name (default TRUE).
The replaced view's comment carries over.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Stores a dplyr pipeline in the lake as a SQL view: the query runs fresh
every time the view is read, so it always reflects the current data.
Views live in the DuckLake catalog itself, which makes them a good home
for shared business logic -- a Python or SQL client of the same lake
sees exactly the same definition.
}
\details{
Read a view back with \code{\link[=get_ducklake_table]{get_ducklake_table()}}, which works for views and
tables alike, and keep piping dplyr verbs onto it. Like tables, views
are versioned: dropping or replacing one is a snapshot like any other.

DuckLake stores a replaced view as a new catalog entry and keys the
comment to the entry, so \verb{CREATE OR REPLACE VIEW} by itself drops the
comment. \code{create_view()} reads the comment first and sets it again, in
the same snapshot as the replacement, the way \code{\link[=replace_table]{replace_table()}} carries a
table's comments over. Reword it with \code{\link[=set_table_comment]{set_table_comment()}}, or clear it
with \code{NULL}.

A view kept in a schema of its own should read its tables by
schema-qualified name: \code{get_ducklake_table("main.cars")}, not \code{"cars"}.
DuckDB resolves an unqualified name from the view's schema and the
session's current database, so the view can fail to bind in a session
where another database is current, and a view that shares its name with
the table it reads (\code{checks.cars} over \code{cars}) reads itself and fails
with a recursion error.
}
\examples{
\dontshow{if (ducklake_extension_available()) withAutoprint(\{ # examplesIf}
lake_dir <- tempfile("view_lake_")
dir.create(lake_dir)
attach_ducklake("view_lake", lake_path = lake_dir)
create_table(mtcars, "cars")

# Encapsulate filtering logic the whole team should share
get_ducklake_table("cars") |>
  dplyr::filter(cyl == 4) |>
  dplyr::select(mpg, cyl, gear) |>
  create_view("v_efficient_cars")

# Reads run the stored query against current data
get_ducklake_table("v_efficient_cars") |> dplyr::collect()

detach_ducklake("view_lake", shutdown = TRUE)
unlink(lake_dir, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=drop_view]{drop_view()}}, \code{\link[=list_ducklake_tables]{list_ducklake_tables()}},
\code{\link[=replace_table]{replace_table()}} to materialize a pipeline as data instead.

Other table operations:
\code{\link[=add_data_files]{add_data_files()}},
\code{\link[=create_schema]{create_schema()}},
\code{\link[=create_table]{create_table()}},
\code{\link[=drop_schema]{drop_schema()}},
\code{\link[=drop_view]{drop_view()}},
\code{\link[=ducklake_exec]{ducklake_exec()}},
\code{\link[=get_ducklake_table]{get_ducklake_table()}},
\code{\link[=get_metadata_table]{get_metadata_table()}},
\code{\link[=list_ducklake_tables]{list_ducklake_tables()}},
\code{\link[=replace_table]{replace_table()}},
\code{\link[=show_ducklake_query]{show_ducklake_query()}}
}
\concept{table operations}
