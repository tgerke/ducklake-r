% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maintenance.R
\name{delete_orphaned_files}
\alias{delete_orphaned_files}
\title{Delete orphaned files}
\usage{
delete_orphaned_files(
  ducklake_name = NULL,
  older_than = NULL,
  cleanup_all = FALSE,
  dry_run = FALSE
)
}
\arguments{
\item{ducklake_name}{Name of the attached DuckLake catalog. If \code{NULL}, the
current database is used.}

\item{older_than}{Only delete files scheduled for deletion before this
timestamp (POSIXct, converted to UTC, or character already in UTC). One
of \code{older_than} or \code{cleanup_all} is required.}

\item{cleanup_all}{If \code{TRUE}, delete all scheduled files regardless of
when they were scheduled.}

\item{dry_run}{If \code{TRUE}, only lists the files that would be deleted.}
}
\value{
A data frame listing the deleted (or deletable) files.
}
\description{
Deletes files sitting in the lake's data path that are not tracked in the
DuckLake metadata at all -- for example, leftovers from a crashed write.
}
\details{
This differs from \code{\link[=cleanup_old_files]{cleanup_old_files()}}, which removes files that \emph{were}
tracked but are scheduled for deletion.

Always run with \code{dry_run = TRUE} first and check the file list. Anything
in the data path that DuckLake does not recognise is fair game, and the
comparison is by exact path string: a data path registered with an
irregularity such as a doubled slash (as R's \code{\link[=tempdir]{tempdir()}} produces on
macOS) makes \emph{live} files look orphaned, and deleting them breaks the
lake.
}
\examples{
\dontrun{
# Always preview orphan deletion first
delete_orphaned_files(dry_run = TRUE, cleanup_all = TRUE)
delete_orphaned_files(cleanup_all = TRUE)
}
}
\seealso{
\code{\link[=cleanup_old_files]{cleanup_old_files()}}, \code{\link[=checkpoint_ducklake]{checkpoint_ducklake()}}

Other maintenance:
\code{\link[=backup_ducklake]{backup_ducklake()}},
\code{\link[=checkpoint_ducklake]{checkpoint_ducklake()}},
\code{\link[=cleanup_old_files]{cleanup_old_files()}},
\code{\link[=expire_snapshots]{expire_snapshots()}},
\code{\link[=flush_inlined_data]{flush_inlined_data()}},
\code{\link[=get_table_info]{get_table_info()}},
\code{\link[=list_ducklake_files]{list_ducklake_files()}},
\code{\link[=merge_adjacent_files]{merge_adjacent_files()}},
\code{\link[=plot_table_files]{plot_table_files()}},
\code{\link[=rewrite_data_files]{rewrite_data_files()}}
}
\concept{maintenance}
