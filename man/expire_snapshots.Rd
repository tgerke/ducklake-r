% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maintenance.R
\name{expire_snapshots}
\alias{expire_snapshots}
\title{Expire old snapshots}
\usage{
expire_snapshots(
  ducklake_name = NULL,
  older_than = NULL,
  versions = NULL,
  dry_run = FALSE
)
}
\arguments{
\item{ducklake_name}{Name of the attached DuckLake catalog. If \code{NULL}, the
current database is used.}

\item{older_than}{Expire all snapshots older than this timestamp (POSIXct
or character in ISO 8601 format). POSIXct values are converted to UTC,
which is how DuckLake records snapshot times; character values must
already be UTC. At least one of \code{older_than} or \code{versions} must be
provided.}

\item{versions}{Integer vector of specific snapshot ids to expire (see
\code{\link[=list_table_snapshots]{list_table_snapshots()}}).}

\item{dry_run}{If \code{TRUE}, only lists the snapshots that would be expired
without expiring them.}
}
\value{
A data frame listing the expired (or, with \code{dry_run = TRUE},
expirable) snapshots.
}
\description{
Removes old snapshots from the DuckLake catalog. Expiring snapshots gives
up the ability to time-travel to them, and schedules the data files that
only they referenced for deletion.
}
\details{
Expiring snapshots does not delete any files by itself: files that are no
longer referenced are merely scheduled for deletion. Run
\code{\link[=cleanup_old_files]{cleanup_old_files()}} afterwards to reclaim the storage, or let
\code{\link[=checkpoint_ducklake]{checkpoint_ducklake()}} handle both steps.

The most recent snapshot can never be expired.
}
\examples{
\dontrun{
# Preview what a one-week retention policy would remove
expire_snapshots(older_than = Sys.time() - 7 * 24 * 60 * 60, dry_run = TRUE)

# Expire it for real, then reclaim the storage
expire_snapshots(older_than = Sys.time() - 7 * 24 * 60 * 60)
cleanup_old_files(cleanup_all = TRUE)

# Expire two specific snapshots
expire_snapshots(versions = c(2, 3))
}
}
\seealso{
\code{\link[=cleanup_old_files]{cleanup_old_files()}}, \code{\link[=checkpoint_ducklake]{checkpoint_ducklake()}},
\code{\link[=list_table_snapshots]{list_table_snapshots()}}

Other maintenance:
\code{\link[=backup_ducklake]{backup_ducklake()}},
\code{\link[=checkpoint_ducklake]{checkpoint_ducklake()}},
\code{\link[=cleanup_old_files]{cleanup_old_files()}},
\code{\link[=delete_orphaned_files]{delete_orphaned_files()}},
\code{\link[=flush_inlined_data]{flush_inlined_data()}},
\code{\link[=get_table_info]{get_table_info()}},
\code{\link[=list_ducklake_files]{list_ducklake_files()}},
\code{\link[=merge_adjacent_files]{merge_adjacent_files()}},
\code{\link[=plot_table_files]{plot_table_files()}},
\code{\link[=rewrite_data_files]{rewrite_data_files()}}
}
\concept{maintenance}
