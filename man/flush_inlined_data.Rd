% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_inlining.R
\name{flush_inlined_data}
\alias{flush_inlined_data}
\title{Flush inlined data to Parquet files}
\usage{
flush_inlined_data(ducklake_name = NULL, table_name = NULL, schema_name = NULL)
}
\arguments{
\item{ducklake_name}{Name of the attached DuckLake catalog. If \code{NULL}, the
current database is used.}

\item{table_name}{Optional table name. When provided, only flushes inlined
data for that table.}

\item{schema_name}{Optional schema name. When provided, only flushes inlined
data for tables in that schema.}
}
\value{
A data frame with columns \code{schema_name}, \code{table_name}, and
\code{rows_flushed}. Tables with no inlined data are omitted.
}
\description{
Materialises data that has been stored inline in the catalog database into
Parquet files on the data path. This includes both inlined inserts and
inlined deletions.
}
\details{
Flushing writes inlined rows to consolidated Parquet files and cleans up the
inlined data tables. Time-travel information is preserved: flushed rows that
had been deleted will produce a partial deletion file with snapshot metadata.

Tables with \code{auto_compact} set to \code{FALSE} are skipped when flushing an
entire lake or schema. Use an explicit \code{table_name} to flush those tables.

If a table has a sort order defined, the flushed Parquet file will be sorted
by those keys.
}
\examples{
\dontrun{
# Flush everything
flush_inlined_data()

# Flush a specific table
flush_inlined_data(table_name = "readings")

# Flush a specific schema
flush_inlined_data(schema_name = "staging")
}
}
\seealso{
\code{\link[=set_inlining_row_limit]{set_inlining_row_limit()}}, \code{\link[=checkpoint_ducklake]{checkpoint_ducklake()}}
}
