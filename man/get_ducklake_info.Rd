% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lake_info.R
\name{get_ducklake_info}
\alias{get_ducklake_info}
\title{Describe an attached DuckLake}
\usage{
get_ducklake_info(ducklake_name = NULL)
}
\arguments{
\item{ducklake_name}{Optional name of the attached DuckLake catalog. If
\code{NULL}, the current database is used.}
}
\value{
A one-row data frame with \code{ducklake_name}, \code{backend},
\code{catalog} (the catalog file or connection string), \code{data_path},
\code{format_version}, \code{extension_version}, \code{encrypted}, and
\code{current_snapshot}.
}
\description{
One row of facts about a lake: which catalog backend it uses and where
the catalog and data live, the DuckLake format version the catalog was
written in, the extension version reading it, whether its Parquet files
are encrypted, and the current snapshot id. Handy at the top of a report
or a pipeline log, and the first thing to look at when a lake behaves
unexpectedly.
}
\examples{
\dontshow{if (ducklake_extension_available()) withAutoprint(\{ # examplesIf}
lake_dir <- tempfile("info_lake_")
dir.create(lake_dir)
attach_ducklake("info_lake", lake_path = lake_dir)
create_table(mtcars, "cars")

get_ducklake_info()

detach_ducklake("info_lake", shutdown = TRUE)
unlink(lake_dir, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_ducklake_options]{get_ducklake_options()}} for the options set on the lake,
\code{\link[=list_ducklake_tables]{list_ducklake_tables()}} for what it holds.

Other connection management:
\code{\link[=attach_ducklake]{attach_ducklake()}},
\code{\link[=create_storage_secret]{create_storage_secret()}},
\code{\link[=detach_ducklake]{detach_ducklake()}},
\code{\link[=ducklake_extension_available]{ducklake_extension_available()}},
\code{\link[=get_ducklake_backend]{get_ducklake_backend()}},
\code{\link[=get_ducklake_connection]{get_ducklake_connection()}},
\code{\link[=install_ducklake]{install_ducklake()}},
\code{\link[=set_ducklake_connection]{set_ducklake_connection()}}
}
\concept{connection management}
