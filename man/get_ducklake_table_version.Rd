% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time_travel.R
\name{get_ducklake_table_version}
\alias{get_ducklake_table_version}
\title{Query a table at a specific version/snapshot}
\usage{
get_ducklake_table_version(table_name, version, conn = NULL)
}
\arguments{
\item{table_name}{The name of the table to query}

\item{version}{The snapshot_id to query (get this from \code{list_table_snapshots()})}

\item{conn}{Optional DuckDB connection object. If not provided, uses the default ducklake connection.}
}
\value{
A lazy table (class \code{tbl_ducklake}) that works with dplyr verbs.
Like \code{\link[=get_ducklake_table]{get_ducklake_table()}}, collecting it restores stored column
labels.
}
\description{
Retrieves data from a DuckLake table at a specific snapshot ID using DuckLake's
AT (VERSION => ...) syntax.
}
\details{
This function allows you to query a specific snapshot of a table using its snapshot_id.
This uses the syntax: \code{SELECT * FROM table AT (VERSION => snapshot_id)}

Each time you create or modify a table within a transaction, DuckLake creates a new
snapshot with a unique snapshot_id. Note that snapshot_id and schema_version are
typically the same value - both represent the snapshot identifier.

Use \code{list_table_snapshots(table_name)} to see all available snapshots and their IDs.
}
\examples{
\dontshow{if (ducklake_extension_available()) withAutoprint(\{ # examplesIf}
lake_dir <- tempfile("version_lake_")
dir.create(lake_dir)
attach_ducklake("version_lake", lake_path = lake_dir)
create_table(data.frame(id = 1:3, amount = c(10, 20, 30)), "orders")

rows_insert(
  get_ducklake_table("orders"),
  data.frame(id = 4L, amount = 40),
  by = "id"
)

# Get available snapshots
snapshots <- list_table_snapshots("orders")

# Query the first snapshot version
get_ducklake_table_version("orders", snapshots$snapshot_id[1]) |>
  dplyr::collect()

detach_ducklake("version_lake", shutdown = TRUE)
unlink(lake_dir, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
Other time travel:
\code{\link[=get_ducklake_table_asof]{get_ducklake_table_asof()}},
\code{\link[=get_table_changes]{get_table_changes()}},
\code{\link[=list_table_snapshots]{list_table_snapshots()}},
\code{\link[=plot_snapshots]{plot_snapshots()}},
\code{\link[=plot_table_changes]{plot_table_changes()}},
\code{\link[=restore_table_version]{restore_table_version()}}
}
\concept{time travel}
