% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comments.R
\name{get_table_comments}
\alias{get_table_comments}
\title{Read table, view, and column comments from a DuckLake catalog}
\usage{
get_table_comments(table_name = NULL, ducklake_name = NULL)
}
\arguments{
\item{table_name}{Optional table (or view) name to filter to.}

\item{ducklake_name}{Optional name of the attached DuckLake catalog. If
\code{NULL}, the current database is used.}
}
\value{
A data frame with one row per comment: \code{object_type}
(\code{"table"}, \code{"view"}, or \code{"column"}), \code{table_name}, \code{column_name}
(\code{NA} for tables and views), and \code{comment}. Zero rows when nothing is
commented.
}
\description{
Returns the current comments stored in the lake -- via
\code{\link[=set_table_comment]{set_table_comment()}}, \code{\link[=set_column_comments]{set_column_comments()}}, \code{\link[=create_table]{create_table()}}'s label
sync, or any other client -- as a tidy data frame.
}
\examples{
\dontshow{if (ducklake_extension_available()) withAutoprint(\{ # examplesIf}
lake_dir <- tempfile("readcomment_lake_")
dir.create(lake_dir)
attach_ducklake("readcomment_lake", lake_path = lake_dir)
create_table(mtcars, "cars")
set_table_comment("cars", "Motor Trend road tests")
set_column_comments("cars", mpg = "Miles per US gallon")

# Everything documented in the lake
get_table_comments()

# One table's documentation
get_table_comments("cars")

detach_ducklake("readcomment_lake", shutdown = TRUE)
unlink(lake_dir, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_metadata_table]{get_metadata_table()}} for the raw \code{ducklake_tag} and
\code{ducklake_column_tag} catalog tables.

Other table documentation:
\code{\link[=set_column_comments]{set_column_comments()}},
\code{\link[=set_table_comment]{set_table_comment()}}
}
\concept{table documentation}
