% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_info.R
\name{get_table_info}
\alias{get_table_info}
\title{Get file statistics for the tables in a lake}
\usage{
get_table_info(table_name = NULL, ducklake_name = NULL, conn = NULL)
}
\arguments{
\item{table_name}{Optional table name. When provided, only that table's
row is returned.}

\item{ducklake_name}{Name of the attached DuckLake catalog. If \code{NULL}, the
current database is used.}

\item{conn}{Optional DuckDB connection object. If not provided, uses the
default ducklake connection.}
}
\value{
A data frame with one row per table and columns \code{table_name},
\code{schema_id}, \code{table_id}, \code{table_uuid}, \code{file_count}, \code{file_size_bytes},
\code{delete_file_count}, and \code{delete_file_size_bytes}.
}
\description{
Returns per-table storage statistics from the DuckLake catalog: how many
Parquet data files each table has and their total size, plus the same for
delete files.
}
\details{
These statistics are the raw material for storage maintenance decisions:
many small files are worth compacting with \code{\link[=merge_adjacent_files]{merge_adjacent_files()}}, and a
growing delete-file share is a sign to run \code{\link[=rewrite_data_files]{rewrite_data_files()}}. Rows
that are still inlined in the catalog (see \code{\link[=set_inlining_row_limit]{set_inlining_row_limit()}}) are
not in any Parquet file yet, so small recent writes may not show up in the
counts until \code{\link[=flush_inlined_data]{flush_inlined_data()}} writes them out.

This wraps DuckLake's \code{ducklake_table_info()} function.
}
\examples{
\dontrun{
# File statistics for every table in the lake
get_table_info()

# Just one table
get_table_info("my_table")
}
}
\seealso{
\code{\link[=plot_table_files]{plot_table_files()}}, \code{\link[=merge_adjacent_files]{merge_adjacent_files()}},
\code{\link[=rewrite_data_files]{rewrite_data_files()}}

Other maintenance:
\code{\link[=backup_ducklake]{backup_ducklake()}},
\code{\link[=checkpoint_ducklake]{checkpoint_ducklake()}},
\code{\link[=cleanup_old_files]{cleanup_old_files()}},
\code{\link[=delete_orphaned_files]{delete_orphaned_files()}},
\code{\link[=expire_snapshots]{expire_snapshots()}},
\code{\link[=flush_inlined_data]{flush_inlined_data()}},
\code{\link[=merge_adjacent_files]{merge_adjacent_files()}},
\code{\link[=plot_table_files]{plot_table_files()}},
\code{\link[=rewrite_data_files]{rewrite_data_files()}}
}
\concept{maintenance}
