% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/partitioning.R
\name{get_table_partitions}
\alias{get_table_partitions}
\title{List the partitioning keys of tables in a lake}
\usage{
get_table_partitions(table_name = NULL, ducklake_name = NULL)
}
\arguments{
\item{table_name}{Optional table name to filter to a single table.}

\item{ducklake_name}{Optional name of the attached DuckLake catalog. If
\code{NULL}, the current database is used.}
}
\value{
A data frame with one row per partition key: \code{table_name},
\code{partition_key_index}, \code{column_name}, and \code{transform} (e.g.
\code{"identity"} or \code{"year"}). Zero rows when nothing is partitioned.
}
\description{
Reads the current partitioning keys from the DuckLake metadata catalog.
}
\examples{
\dontshow{if (ducklake_extension_available()) withAutoprint(\{ # examplesIf}
lake_dir <- tempfile("getpart_lake_")
dir.create(lake_dir)
attach_ducklake("getpart_lake", lake_path = lake_dir)
create_table(mtcars, "cars")

set_table_partitioning("cars", "cyl")

# All partitioned tables in the lake
get_table_partitions()

# Keys for one table
get_table_partitions("cars")

detach_ducklake("getpart_lake", shutdown = TRUE)
unlink(lake_dir, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=set_table_partitioning]{set_table_partitioning()}}, \code{\link[=get_metadata_table]{get_metadata_table()}}

Other partitioning:
\code{\link[=reset_table_partitioning]{reset_table_partitioning()}},
\code{\link[=set_table_partitioning]{set_table_partitioning()}}
}
\concept{partitioning}
