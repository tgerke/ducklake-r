% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sorting.R
\name{get_table_sorting}
\alias{get_table_sorting}
\title{List the sort keys of tables in a lake}
\usage{
get_table_sorting(table_name = NULL, ducklake_name = NULL)
}
\arguments{
\item{table_name}{Optional table name to filter to a single table.}

\item{ducklake_name}{Optional name of the attached DuckLake catalog. If
\code{NULL}, the current database is used.}
}
\value{
A data frame with one row per sort key: \code{table_name},
\code{sort_key_index}, \code{expression}, \code{sort_direction} (\code{"ASC"} or \code{"DESC"}),
and \code{null_order} (\code{"NULLS_FIRST"} or \code{"NULLS_LAST"}). Zero rows when
nothing is sorted.
}
\description{
Reads the current sort order from the DuckLake metadata catalog, the
counterpart of \code{\link[=get_table_partitions]{get_table_partitions()}} for \code{\link[=set_table_sorting]{set_table_sorting()}}.
}
\examples{
\dontshow{if (ducklake_extension_available()) withAutoprint(\{ # examplesIf}
lake_dir <- tempfile("getsort_lake_")
dir.create(lake_dir)
attach_ducklake("getsort_lake", lake_path = lake_dir)
create_table(mtcars, "cars")

set_table_sorting("cars", c("cyl ASC", "mpg DESC"))

# All sorted tables in the lake
get_table_sorting()

# Keys for one table
get_table_sorting("cars")

detach_ducklake("getsort_lake", shutdown = TRUE)
unlink(lake_dir, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=set_table_sorting]{set_table_sorting()}}, \code{\link[=get_table_partitions]{get_table_partitions()}}

Other sorting:
\code{\link[=reset_table_sorting]{reset_table_sorting()}},
\code{\link[=set_table_sorting]{set_table_sorting()}}
}
\concept{sorting}
