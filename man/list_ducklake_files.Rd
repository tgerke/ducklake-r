% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_files.R
\name{list_ducklake_files}
\alias{list_ducklake_files}
\title{List the data files backing a DuckLake table}
\usage{
list_ducklake_files(
  table_name,
  schema_name = NULL,
  snapshot_version = NULL,
  snapshot_time = NULL,
  ducklake_name = NULL
)
}
\arguments{
\item{table_name}{The table whose files to list.}

\item{schema_name}{Optional schema containing the table (defaults to the
lake's \code{main} schema).}

\item{snapshot_version}{Optional snapshot id: list the files as of that
snapshot. Mutually exclusive with \code{snapshot_time}.}

\item{snapshot_time}{Optional POSIXct or UTC timestamp string: list the
files as of that moment. Mutually exclusive with \code{snapshot_version}.}

\item{ducklake_name}{Optional name of the attached DuckLake catalog. If
\code{NULL}, the current database is used.}
}
\value{
A data frame with one row per data file, including \code{data_file},
\code{data_file_size_bytes}, and the associated \code{delete_file} columns
(\code{NA} when a file has no deletes).
}
\description{
Returns the Parquet data files (and any delete files) that make up a
table, optionally as of a past snapshot.
}
\details{
Wraps \code{ducklake_list_files()}. For per-table file counts and sizes across
the whole lake, see \code{\link[=get_table_info]{get_table_info()}}; for a picture of storage layout,
see \code{\link[=plot_table_files]{plot_table_files()}}.
}
\examples{
\dontshow{if (ducklake_extension_available()) withAutoprint(\{ # examplesIf}
lake_dir <- tempfile("listfiles_lake_")
dir.create(lake_dir)
attach_ducklake("listfiles_lake", lake_path = lake_dir)
create_table(mtcars, "cars")
rows_insert(
  get_ducklake_table("cars"),
  data.frame(mpg = 30, cyl = 4),
  by = "mpg"
)

# Files behind a table right now
list_ducklake_files("cars")

# Files as of an earlier snapshot
first <- min(list_table_snapshots("cars")$snapshot_id)
list_ducklake_files("cars", snapshot_version = first)

detach_ducklake("listfiles_lake", shutdown = TRUE)
unlink(lake_dir, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=add_data_files]{add_data_files()}}, \code{\link[=get_table_info]{get_table_info()}}

Other maintenance:
\code{\link[=backup_ducklake]{backup_ducklake()}},
\code{\link[=checkpoint_ducklake]{checkpoint_ducklake()}},
\code{\link[=cleanup_old_files]{cleanup_old_files()}},
\code{\link[=delete_orphaned_files]{delete_orphaned_files()}},
\code{\link[=expire_snapshots]{expire_snapshots()}},
\code{\link[=flush_inlined_data]{flush_inlined_data()}},
\code{\link[=get_table_info]{get_table_info()}},
\code{\link[=merge_adjacent_files]{merge_adjacent_files()}},
\code{\link[=plot_table_files]{plot_table_files()}},
\code{\link[=rewrite_data_files]{rewrite_data_files()}}
}
\concept{maintenance}
