% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maintenance.R
\name{merge_adjacent_files}
\alias{merge_adjacent_files}
\title{Merge adjacent Parquet files}
\usage{
merge_adjacent_files(
  ducklake_name = NULL,
  table_name = NULL,
  schema_name = NULL,
  max_compacted_files = NULL,
  min_file_size = NULL,
  max_file_size = NULL
)
}
\arguments{
\item{ducklake_name}{Name of the attached DuckLake catalog. If \code{NULL}, the
current database is used.}

\item{table_name}{Optional table name. When provided, only that table is
compacted.}

\item{schema_name}{Optional schema name. When provided, only tables in
that schema are compacted.}

\item{max_compacted_files}{Optional cap on the number of compaction
operations per table in a single call.}

\item{min_file_size}{Optional minimum file size in bytes; smaller files
are excluded from merging.}

\item{max_file_size}{Optional maximum file size in bytes; files at or
above this size are excluded. Defaults to the lake's target file size.}
}
\value{
A data frame with one row per output file (columns
\code{schema_name}, \code{table_name}, \code{files_processed}, \code{files_created}).
}
\description{
Compacts small adjacent Parquet files into larger ones. Frequent small
inserts each write their own file; merging keeps file counts down and
scans fast.
}
\details{
Merging does not delete the original small files -- they may still be
referenced by older snapshots. They are scheduled for deletion once no
snapshot references them; run \code{\link[=cleanup_old_files]{cleanup_old_files()}} to remove them.
}
\examples{
\dontrun{
# Compact the whole lake
merge_adjacent_files()

# Compact one table, only touching files under 10 MB
merge_adjacent_files(table_name = "readings", max_file_size = 10e6)
}
}
\seealso{
\code{\link[=cleanup_old_files]{cleanup_old_files()}}, \code{\link[=checkpoint_ducklake]{checkpoint_ducklake()}},
\code{\link[=flush_inlined_data]{flush_inlined_data()}}

Other maintenance:
\code{\link[=backup_ducklake]{backup_ducklake()}},
\code{\link[=checkpoint_ducklake]{checkpoint_ducklake()}},
\code{\link[=cleanup_old_files]{cleanup_old_files()}},
\code{\link[=delete_orphaned_files]{delete_orphaned_files()}},
\code{\link[=expire_snapshots]{expire_snapshots()}},
\code{\link[=flush_inlined_data]{flush_inlined_data()}},
\code{\link[=rewrite_data_files]{rewrite_data_files()}}
}
\concept{maintenance}
