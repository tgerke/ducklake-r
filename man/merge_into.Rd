% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_into.R
\name{merge_into}
\alias{merge_into}
\title{Merge a source table into a DuckLake table}
\usage{
merge_into(
  target,
  source,
  by,
  when_matched = c("update", "delete", "nothing"),
  when_not_matched = c("insert", "nothing"),
  matched_condition = NULL,
  not_matched_condition = NULL,
  delete_missing = FALSE,
  .quiet = TRUE
)
}
\arguments{
\item{target}{The table to modify: a table from \code{\link[=get_ducklake_table]{get_ducklake_table()}} or
a table name.}

\item{source}{The rows to merge in: a data frame or a lazy table on the
same connection.}

\item{by}{Character vector of key column(s) to match on. Rows with \code{NULL}
key values never match.}

\item{when_matched}{What to do with target rows that match a source row:
\code{"update"} (default) sets the columns the two tables share, \code{"delete"}
removes the row, \code{"nothing"} leaves it alone.}

\item{when_not_matched}{What to do with source rows that match no target
row: \code{"insert"} (default) adds them, \code{"nothing"} skips them.}

\item{matched_condition}{Optional SQL expression further restricting the
\code{when_matched} action, written against the aliases \code{target} and
\code{source}, e.g. \code{"source.amt > target.amt"}.}

\item{not_matched_condition}{Optional SQL expression further restricting
the \code{when_not_matched} action.}

\item{delete_missing}{Also delete target rows that have no match in the
source (\verb{WHEN NOT MATCHED BY SOURCE THEN DELETE}). Combined with the
update action this synchronizes \code{target} to \code{source}.}

\item{.quiet}{Logical, whether to suppress the row-count message
(default TRUE).}
}
\value{
The number of affected rows, invisibly.
}
\description{
Runs a SQL \verb{MERGE INTO} statement: rows of \code{target} are matched against
rows of \code{source} on the \code{by} columns, then updated, deleted, or left
alone, while unmatched source rows can be inserted and target rows
missing from the source can be removed. The whole operation is atomic --
one snapshot, with row lineage preserved in the change feed.
}
\details{
This is not a join. Joins (\code{left_join()} and friends) read from the lake
and build a new result without touching either table; \code{merge_into()}
changes the rows of \code{target} in place. For the common update-or-insert
case, reach for \code{\link[=rows_upsert]{rows_upsert()}} first -- \code{merge_into()} is for the cases
it cannot express: conditional clauses, deletes of matched rows, and
synchronizing a table with a staging source.
\subsection{Choosing how to change a table}{
\itemize{
\item To look up or combine data for analysis, use dplyr joins
(\code{left_join()} and friends). Joins read from the lake and build a new
result; they never modify a lake table.
\item To append, correct, or remove specific rows, use \code{\link[=rows_insert]{rows_insert()}},
\code{\link[=rows_update]{rows_update()}}, or \code{\link[=rows_delete]{rows_delete()}}. Each call is a single SQL statement
against the existing table -- no data leaves the database, and with data
inlining enabled (DuckLake's default) small changes land in the catalog
without creating tiny Parquet files.
\item To update rows that exist and insert the ones that don't in one atomic
statement, use \code{\link[=rows_upsert]{rows_upsert()}}.
\item For conditional merge logic or deletes driven by a staging table, use
\code{\link[=merge_into]{merge_into()}}.
\item To change a table's shape without touching its data -- add, drop, or
rename columns, widen a type -- use the schema evolution family
(\code{\link[=add_table_column]{add_table_column()}} and friends): metadata-only changes that rewrite
nothing.
\item For bulk transformations that touch most rows, use \code{\link[=replace_table]{replace_table()}}. It
collects the transformed data into R and rewrites the whole table --
heavier than the row operations, and it resets the row lineage that the
in-place operations preserve in the change feed.
}

A tempting alternative -- joining the source to the table and calling
\code{\link[=replace_table]{replace_table()}} on the result -- rewrites every row and records the
change as a wholesale replacement. \code{merge_into()} touches only the
affected rows, so \code{\link[=get_table_changes]{get_table_changes()}} afterward shows exactly which
rows were inserted, updated, or deleted.
}

\subsection{Conditions are SQL}{

\code{matched_condition} and \code{not_matched_condition} are raw SQL expressions,
not dplyr code. They are pasted into the statement as written (only a
\verb{;} is rejected), so build them from trusted input only.
}

\subsection{DuckLake-specific behavior}{
\itemize{
\item Update sets the columns present in both tables (minus \code{by}); inserted
rows receive the column default (\code{NULL} when none is defined) in target
columns the source lacks.
\item \verb{MERGE ... RETURNING} is not implemented by DuckLake.
\item DuckLake currently supports one update/delete action per MERGE
statement. \code{delete_missing = TRUE} together with a \code{when_matched}
action therefore runs as a MERGE plus a \code{DELETE} of the unmatched rows,
wrapped in one transaction (a single snapshot). When you already opened
a transaction, both statements simply join it.
\item When several source rows share a \code{by} key, matched updates apply in an
unspecified order; keep source keys unique.
}
}
}
\examples{
\dontrun{
# Update only when the source amount is higher; insert new ids
merge_into(
  get_ducklake_table("sales"), new_sales, by = "sls_id",
  matched_condition = "source.sls_amt > target.sls_amt"
)

# Synchronize to a staging table: upsert + drop rows gone from the source
merge_into(
  "sales", staging_sales, by = "sls_id",
  delete_missing = TRUE
)

# Remove rows flagged in the source
merge_into(
  "sales", withdrawn, by = "sls_id",
  when_matched = "delete", when_not_matched = "nothing"
)
}
}
\seealso{
\code{\link[=rows_upsert]{rows_upsert()}} for the plain update-or-insert case;
\code{\link[=get_table_changes]{get_table_changes()}} to inspect what a merge did.

Other row operations:
\code{\link[=rows_delete]{rows_delete()}},
\code{\link[=rows_insert]{rows_insert()}},
\code{\link[=rows_update]{rows_update()}},
\code{\link[=rows_upsert]{rows_upsert()}}
}
\concept{row operations}
