% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_table_changes.R
\name{plot_table_changes}
\alias{plot_table_changes}
\title{Plot the rows changed in each snapshot of a table}
\usage{
plot_table_changes(table_name, ducklake_name = NULL, conn = NULL)
}
\arguments{
\item{table_name}{The name of the table to plot.}

\item{ducklake_name}{The name of the ducklake (database) to query. If NULL, will attempt to infer from current database.}

\item{conn}{Optional DuckDB connection object. If not provided, uses the default ducklake connection.}
}
\value{
A ggplot object, which can be further customized with ggplot2
functions
}
\description{
Draws a table's change volume as a diverging bar chart: one bar per
snapshot, with rows inserted or updated above the axis and rows deleted
below it. A companion to \code{\link[=plot_snapshots]{plot_snapshots()}}, which shows \emph{when} and \emph{what
kind} of changes happened; this shows \emph{how much} changed each time.
}
\details{
Requires the ggplot2 package (listed in Suggests). Row counts come from
DuckLake's data change feed via \code{\link[=get_table_changes]{get_table_changes()}}. An update appears
in the feed as a before and an after image of the row; it is counted once
here. Snapshots that touched the table without changing rows (a schema
change, for example) keep their slot on the axis with no bar.
}
\examples{
\dontrun{
# Rows inserted, updated, and deleted per snapshot
plot_table_changes("my_table")

# Customize the result like any ggplot
plot_table_changes("my_table") +
  ggplot2::theme_classic()
}
}
\seealso{
Other time travel:
\code{\link[=get_ducklake_table_asof]{get_ducklake_table_asof()}},
\code{\link[=get_ducklake_table_version]{get_ducklake_table_version()}},
\code{\link[=get_table_changes]{get_table_changes()}},
\code{\link[=list_table_snapshots]{list_table_snapshots()}},
\code{\link[=plot_snapshots]{plot_snapshots()}},
\code{\link[=restore_table_version]{restore_table_version()}}
}
\concept{time travel}
