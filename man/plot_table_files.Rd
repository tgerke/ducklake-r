% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_table_files.R
\name{plot_table_files}
\alias{plot_table_files}
\title{Plot the file layout of a lake}
\usage{
plot_table_files(ducklake_name = NULL, conn = NULL)
}
\arguments{
\item{ducklake_name}{The name of the ducklake (database) to query. If NULL, will attempt to infer from current database.}

\item{conn}{Optional DuckDB connection object. If not provided, uses the default ducklake connection.}
}
\value{
A ggplot object, which can be further customized with ggplot2
functions
}
\description{
Draws each table's storage footprint as a horizontal bar: total bytes of
Parquet data files, with delete files stacked in a second color, and a
label giving the file count and average file size. Useful for spotting
fragmentation -- many small files -- before it slows scans down.
}
\details{
Requires the ggplot2 package (listed in Suggests). File statistics come
from \code{\link[=get_table_info]{get_table_info()}}. Tables whose rows are still inlined in the
catalog have no data files yet and show an empty bar; run
\code{\link[=flush_inlined_data]{flush_inlined_data()}} to write them out. Many small files can be
compacted with \code{\link[=merge_adjacent_files]{merge_adjacent_files()}}, and a large delete-file share is
a sign to run \code{\link[=rewrite_data_files]{rewrite_data_files()}}.
}
\examples{
\dontrun{
# File counts and sizes for every table in the lake
plot_table_files()

# Customize the result like any ggplot
plot_table_files() +
  ggplot2::theme_classic()
}
}
\seealso{
Other maintenance:
\code{\link[=backup_ducklake]{backup_ducklake()}},
\code{\link[=checkpoint_ducklake]{checkpoint_ducklake()}},
\code{\link[=cleanup_old_files]{cleanup_old_files()}},
\code{\link[=delete_orphaned_files]{delete_orphaned_files()}},
\code{\link[=expire_snapshots]{expire_snapshots()}},
\code{\link[=flush_inlined_data]{flush_inlined_data()}},
\code{\link[=get_table_info]{get_table_info()}},
\code{\link[=merge_adjacent_files]{merge_adjacent_files()}},
\code{\link[=rewrite_data_files]{rewrite_data_files()}}
}
\concept{maintenance}
