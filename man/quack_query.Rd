% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quack.R
\name{quack_query}
\alias{quack_query}
\title{Run a one-off query against a remote Quack server}
\usage{
quack_query(uri, query, token = NULL, disable_ssl = FALSE)
}
\arguments{
\item{uri}{Address of the Quack server, for example \code{"quack:localhost"}.}

\item{query}{A SQL query string to run on the server.}

\item{token}{Authentication token expected by the server. If \code{NULL}, a Quack
secret is used if one exists.}

\item{disable_ssl}{Connect over plain HTTP instead of HTTPS (default \code{FALSE}).}
}
\value{
A data.frame with the query result.
}
\description{
Sends a single SQL query to a Quack server and returns the result as a
data.frame. Unlike \code{\link[=attach_quack]{attach_quack()}}, this does not attach the remote catalog,
so it is a quick way to pull a result without changing the session state.
}
\examples{
\dontrun{
quack_query(
  "quack:data.example.org",
  "SELECT count(*) FROM adsl",
  token = "super_secret"
)
}
}
\seealso{
\code{\link[=attach_quack]{attach_quack()}}
}
