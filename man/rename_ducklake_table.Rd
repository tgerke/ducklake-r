% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema_evolution.R
\name{rename_ducklake_table}
\alias{rename_ducklake_table}
\title{Rename a DuckLake table}
\usage{
rename_ducklake_table(from, to)
}
\arguments{
\item{from}{Current table name.}

\item{to}{New table name.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Renames a table in place with \verb{ALTER TABLE ... RENAME TO}, a
metadata-only change.
}
\details{
History survives the rename, but snapshots from before it stay
associated with the old name: \code{get_ducklake_table_version()} on the new
name reaches back only as far as the rename, while the old name still
serves the earlier snapshots.
}
\examples{
\dontshow{if (ducklake_extension_available()) withAutoprint(\{ # examplesIf}
lake_dir <- tempfile("renametbl_lake_")
dir.create(lake_dir)
attach_ducklake("renametbl_lake", lake_path = lake_dir)
create_table(mtcars, "cars")

rename_ducklake_table("cars", "cars_daily")

detach_ducklake("renametbl_lake", shutdown = TRUE)
unlink(lake_dir, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=rename_table_column]{rename_table_column()}}

Other schema evolution:
\code{\link[=add_table_column]{add_table_column()}},
\code{\link[=drop_table_column]{drop_table_column()}},
\code{\link[=rename_table_column]{rename_table_column()}},
\code{\link[=set_column_not_null]{set_column_not_null()}},
\code{\link[=set_column_type]{set_column_type()}}
}
\concept{schema evolution}
