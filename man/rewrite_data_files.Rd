% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maintenance.R
\name{rewrite_data_files}
\alias{rewrite_data_files}
\title{Rewrite data files with many deletes}
\usage{
rewrite_data_files(
  ducklake_name = NULL,
  table_name = NULL,
  delete_threshold = NULL
)
}
\arguments{
\item{ducklake_name}{Name of the attached DuckLake catalog. If \code{NULL}, the
current database is used.}

\item{table_name}{Optional table name. When provided, only that table's
files are rewritten.}

\item{delete_threshold}{Optional fraction of deleted rows (between 0 and
\enumerate{
\item above which a file is rewritten. DuckLake's default is 0.95.
}}
}
\value{
A data frame with one row per output file (columns
\code{schema_name}, \code{table_name}, \code{files_processed}, \code{files_created}).
}
\description{
Rewrites Parquet files whose rows have mostly been deleted. Deletes in
DuckLake are recorded in separate delete files; heavily-deleted data files
slow reads down until they are rewritten without the dead rows.
}
\details{
The rewritten originals are scheduled for deletion once no snapshot
references them; run \code{\link[=cleanup_old_files]{cleanup_old_files()}} to remove them.
}
\examples{
\dontrun{
# Rewrite any file that is at least half deleted
rewrite_data_files("my_lake", delete_threshold = 0.5)

# Just one table, with DuckLake's default threshold
rewrite_data_files(table_name = "events")
}
}
\seealso{
\code{\link[=cleanup_old_files]{cleanup_old_files()}}, \code{\link[=checkpoint_ducklake]{checkpoint_ducklake()}}

Other maintenance:
\code{\link[=backup_ducklake]{backup_ducklake()}},
\code{\link[=checkpoint_ducklake]{checkpoint_ducklake()}},
\code{\link[=cleanup_old_files]{cleanup_old_files()}},
\code{\link[=delete_orphaned_files]{delete_orphaned_files()}},
\code{\link[=expire_snapshots]{expire_snapshots()}},
\code{\link[=flush_inlined_data]{flush_inlined_data()}},
\code{\link[=merge_adjacent_files]{merge_adjacent_files()}}
}
\concept{maintenance}
