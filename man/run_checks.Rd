% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{run_checks}
\alias{run_checks}
\title{Run the data checks stored in a schema}
\usage{
run_checks(schema_name = "checks", ducklake_name = NULL)
}
\arguments{
\item{schema_name}{The schema that holds the check views (default
\code{"checks"}).}

\item{ducklake_name}{Optional name of the attached DuckLake catalog. If
\code{NULL}, the current database is used.}
}
\value{
A data frame with one row per check, ordered by name: \code{check}
(the view name), \code{label} (the view's comment, \code{NA} without one), and
\code{n_fail} (the number of rows the view returns). Zero rows when the
schema holds no views.
}
\description{
\strong{Experimental.} Counts the rows returned by each view in a schema set
aside for data checks. The convention: a check is a view that returns the
rows breaking a rule, the view's name is the rule's id, and its comment is
the rule's label. Checks written this way live in the DuckLake catalog,
so they are versioned with the data and every client of the lake can run
them. The interface may change while the convention settles.
}
\details{
Write a check with \code{\link[=create_view]{create_view()}} from a pipeline that keeps the failing
rows, such as \code{filter(!(cyl \%in\% c(4, 6, 8)))}, and label it with
\code{\link[=set_table_comment]{set_table_comment()}}. \code{NOT (condition)} is not true for a row where the
condition is \code{NA}, so a missing value passes unless a check of its own
looks for it. Read the table by its schema-qualified name,
\code{get_ducklake_table("main.cars")}: the view then binds whichever database
is current, for this function's \code{ducklake_name} and for other clients of
the lake.

Every view in the schema counts as a check, so keep other views
elsewhere. A view that summarizes, returning a row of totals, reports a
failure every time. A check whose view no longer binds, after a column
rename for instance, is an error and not a pass; DuckDB's message quotes
the line naming the view.

Inside \code{\link[=with_transaction]{with_transaction()}} the counts include the transaction's pending
writes, so a load can be checked before it commits: \code{stop()} when a check
fails and the transaction rolls back. On a lake attached with
\code{snapshot_version} or \code{snapshot_time}, the checks and the data are both
read as of that snapshot.
}
\examples{
\dontshow{if (ducklake_extension_available()) withAutoprint(\{ # examplesIf}
lake_dir <- tempfile("checks_lake_")
dir.create(lake_dir)
attach_ducklake("checks_lake", lake_path = lake_dir)
create_table(mtcars, "cars")

# A check is a view of the rows that break a rule
create_schema("checks")
get_ducklake_table("main.cars") |>
  dplyr::filter(!(cyl \%in\% c(4, 6, 8))) |>
  create_view("checks.cyl_known")
set_table_comment("checks.cyl_known", "cyl is 4, 6, or 8")

run_checks()

detach_ducklake("checks_lake", shutdown = TRUE)
unlink(lake_dir, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=create_view]{create_view()}} and \code{\link[=set_table_comment]{set_table_comment()}} to write a check,
\code{\link[=with_transaction]{with_transaction()}} to gate a load on the result, and
\code{vignette("data-checks")}.
}
