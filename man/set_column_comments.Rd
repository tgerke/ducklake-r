% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comments.R
\name{set_column_comments}
\alias{set_column_comments}
\title{Set column comments on a DuckLake table}
\usage{
set_column_comments(table_name, ...)
}
\arguments{
\item{table_name}{The table whose columns to describe.}

\item{...}{Named comments, e.g. \code{USUBJID = "Unique subject identifier"}.
Use \code{NA} (or \code{NULL}) as a value to clear that column's comment. To
pass a named vector built elsewhere, splice it with
\code{do.call(set_column_comments, c(list("tbl"), as.list(my_labels)))}.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Stores per-column descriptions in the DuckLake catalog with
\verb{COMMENT ON COLUMN}, one \code{name = "comment"} pair per column. For data
with haven/labelled variable labels, \code{\link[=create_table]{create_table()}} can store the
labels automatically; this function adds or revises them afterward --
for example to label a derived variable.
}
\details{
All comments from one call are written in a single transaction, so they
land as one snapshot. Inside \code{\link[=with_transaction]{with_transaction()}} they join the open
transaction instead.
}
\examples{
\dontrun{
set_column_comments(
  "adsl",
  USUBJID = "Unique subject identifier",
  AGEGR1 = "Age group 1",
  SCRATCH = NA  # clear this one
)
}
}
\seealso{
\code{\link[=set_table_comment]{set_table_comment()}}, \code{\link[=get_table_comments]{get_table_comments()}}

Other table documentation:
\code{\link[=get_table_comments]{get_table_comments()}},
\code{\link[=set_table_comment]{set_table_comment()}}
}
\concept{table documentation}
