% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema_evolution.R
\name{set_column_not_null}
\alias{set_column_not_null}
\title{Require or allow NULL values in a column}
\usage{
set_column_not_null(table_name, column_name, not_null = TRUE)
}
\arguments{
\item{table_name}{The table to change.}

\item{column_name}{Name of the column.}

\item{not_null}{\code{TRUE} (the default) to require a value in every row,
\code{FALSE} to allow \code{NULL} again.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Sets or drops a \verb{NOT NULL} constraint with
\verb{ALTER TABLE ... ALTER COLUMN ... SET NOT NULL} (or \verb{DROP NOT NULL}), a
metadata-only change. \verb{NOT NULL} is the one constraint DuckLake
supports: there are no primary keys, unique constraints, or check
constraints, which is why \code{\link[=rows_upsert]{rows_upsert()}} matches on its \code{by} columns
instead of a key. Existing rows must already satisfy the constraint, and
from then on an insert or update that would leave the column \code{NULL} is
refused.
}
\examples{
\dontshow{if (ducklake_extension_available()) withAutoprint(\{ # examplesIf}
lake_dir <- tempfile("notnull_lake_")
dir.create(lake_dir)
attach_ducklake("notnull_lake", lake_path = lake_dir)
create_table(data.frame(id = 1:3, code = c("A", "B", "C")), "sites")

set_column_not_null("sites", "code")

# A row without a code is now refused
try(rows_insert(get_ducklake_table("sites"), data.frame(id = 4L), by = "id"))

set_column_not_null("sites", "code", not_null = FALSE)

detach_ducklake("notnull_lake", shutdown = TRUE)
unlink(lake_dir, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=add_table_column]{add_table_column()}}, \code{\link[=set_column_type]{set_column_type()}}

Other schema evolution:
\code{\link[=add_table_column]{add_table_column()}},
\code{\link[=drop_table_column]{drop_table_column()}},
\code{\link[=rename_ducklake_table]{rename_ducklake_table()}},
\code{\link[=rename_table_column]{rename_table_column()}},
\code{\link[=set_column_type]{set_column_type()}}
}
\concept{schema evolution}
