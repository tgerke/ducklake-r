% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{set_ducklake_option}
\alias{set_ducklake_option}
\title{Set a DuckLake option}
\usage{
set_ducklake_option(
  option,
  value,
  table_name = NULL,
  schema_name = NULL,
  ducklake_name = NULL
)
}
\arguments{
\item{option}{Name of the option, e.g. \code{"parquet_compression"},
\code{"target_file_size"}, \code{"sort_on_insert"}, or
\code{"data_inlining_row_limit"}. See
\url{https://ducklake.select/docs/stable/duckdb/usage/configuration}
for the full list.}

\item{value}{The value to set. Logicals are rendered as \code{true}/\code{false},
numbers as numeric literals, and everything else as a quoted string.}

\item{table_name}{Optional table name to scope the option to one table.}

\item{schema_name}{Optional schema name to scope the option to one schema
(or, together with \code{table_name}, to qualify the table).}

\item{ducklake_name}{Optional name of the attached DuckLake catalog. If
\code{NULL}, the current database is used.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Sets a DuckLake configuration option, either lake-wide or scoped to a
schema or table. Options are persisted in the metadata catalog, so they
survive detach/attach cycles and apply to every client of the lake.
}
\details{
Table-scoped settings override schema-scoped ones, which override the
lake-wide default. Runs \verb{CALL <lake>.set_option(...)}.

Commonly tuned options include \code{parquet_compression} (default
\code{"snappy"}; \code{"zstd"} trades write speed for smaller files),
\code{target_file_size} (default \code{"512MB"}), \code{sort_on_insert} (default
\code{TRUE}; see \code{\link[=set_table_sorting]{set_table_sorting()}}), and \code{require_commit_message}
(default \code{FALSE}).
}
\examples{
\dontrun{
# Smaller files at some write cost, lake-wide
set_ducklake_option("parquet_compression", "zstd")

# Make every snapshot carry a commit message
set_ducklake_option("require_commit_message", TRUE)

# Skip one table during compaction
set_ducklake_option("auto_compact", FALSE, table_name = "audit_log")
}
}
\seealso{
\code{\link[=get_ducklake_options]{get_ducklake_options()}}, \code{\link[=set_inlining_row_limit]{set_inlining_row_limit()}}

Other options:
\code{\link[=get_ducklake_options]{get_ducklake_options()}}
}
\concept{options}
