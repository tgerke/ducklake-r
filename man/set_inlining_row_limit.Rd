% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_inlining.R
\name{set_inlining_row_limit}
\alias{set_inlining_row_limit}
\title{Set the data inlining row limit}
\usage{
set_inlining_row_limit(
  limit,
  table_name = NULL,
  schema_name = NULL,
  ducklake_name = NULL
)
}
\arguments{
\item{limit}{Integer. The maximum number of rows that will be inlined.
Set to \code{0} to disable inlining entirely.}

\item{table_name}{Optional table name. When provided the limit is persisted
for that table in the DuckLake metadata (takes priority over the global
setting).}

\item{schema_name}{Optional schema name. When provided (without
\code{table_name}) the limit is persisted for all tables in that schema.}

\item{ducklake_name}{Optional name of the attached DuckLake catalog.
Required when setting a table- or schema-level override. If \code{NULL}, the
current database is used.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Controls the threshold below which DuckLake stores small inserts and
deletes directly in the catalog database instead of writing Parquet files.
This avoids the "small files problem" common in streaming or frequent-update
workloads.
}
\details{
The limit can be set at three levels (highest priority first):
\enumerate{
\item \strong{Table-level} – persisted in the DuckLake metadata for a specific table
\item \strong{Schema-level} – persisted for all tables in a schema
\item \strong{Global (DuckDB setting)} – applies to all DuckLake connections
}

Data inlining is enabled by default in DuckLake v1.0 with a threshold of 10
rows. Any insert or delete affecting fewer rows than the limit is written to
an inlined table inside the catalog instead of creating a Parquet file.

For streaming or high-frequency-insert workloads, increase the limit (e.g.,
50 or 100). For workloads that always write large batches, the default is
fine or you can disable inlining with \code{limit = 0}.

Use \code{\link[=flush_inlined_data]{flush_inlined_data()}} or \code{\link[=checkpoint_ducklake]{checkpoint_ducklake()}} to materialise inlined
data to Parquet when ready.
}
\examples{
\dontrun{
# Change the global default
set_inlining_row_limit(50)

# Override for a specific table
set_inlining_row_limit(100, table_name = "readings")

# Disable inlining globally
set_inlining_row_limit(0)
}
}
\seealso{
\code{\link[=get_inlining_row_limit]{get_inlining_row_limit()}}, \code{\link[=flush_inlined_data]{flush_inlined_data()}},
\code{\link[=checkpoint_ducklake]{checkpoint_ducklake()}}
}
