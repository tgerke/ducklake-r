% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/partitioning.R
\name{set_table_partitioning}
\alias{set_table_partitioning}
\title{Set partitioning keys for a table}
\usage{
set_table_partitioning(table_name, partition_by)
}
\arguments{
\item{table_name}{The name of the table to partition.}

\item{partition_by}{Character vector of partition expressions. Each entry
must be one of:
\itemize{
\item a column name, e.g. \code{"region"} (identity transform)
\item \code{"year(col)"}, \code{"month(col)"}, \code{"day(col)"}, or \code{"hour(col)"}
for timestamp columns
\item \code{"bucket(n, col)"} for hash bucketing into \code{n} buckets
}}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Declares how newly written data files for a table should be split up.
Partitioning lets DuckLake prune files during query planning, which can
speed up filtered reads on large tables considerably.
}
\details{
Partitioning only affects data written \emph{after} the keys are set;
previously written files keep their layout. To re-partition existing
data, set the keys and then rewrite the table with \code{\link[=replace_table]{replace_table()}}:
the rewrite carries the keys over to the new table and writes every row
through them.

Runs \verb{ALTER TABLE ... SET PARTITIONED BY (...)}. The expressions are
validated against the transforms DuckLake supports before any SQL is
built.
}
\examples{
\dontshow{if (ducklake_extension_available()) withAutoprint(\{ # examplesIf}
lake_dir <- tempfile("part_lake_")
dir.create(lake_dir)
attach_ducklake("part_lake", lake_path = lake_dir)
create_table(mtcars, "cars")

# Plain column partitioning
set_table_partitioning("cars", "cyl")

# Compound key
set_table_partitioning("cars", c("gear", "cyl"))

# Timestamp columns can be split by year(), month(), day(), or hour()

detach_ducklake("part_lake", shutdown = TRUE)
unlink(lake_dir, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=reset_table_partitioning]{reset_table_partitioning()}}, \code{\link[=get_table_partitions]{get_table_partitions()}}

Other partitioning:
\code{\link[=get_table_partitions]{get_table_partitions()}},
\code{\link[=reset_table_partitioning]{reset_table_partitioning()}}
}
\concept{partitioning}
