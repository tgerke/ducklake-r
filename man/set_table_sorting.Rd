% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sorting.R
\name{set_table_sorting}
\alias{set_table_sorting}
\title{Set the sort order of a table}
\usage{
set_table_sorting(table_name, sort_by)
}
\arguments{
\item{table_name}{The name of the table to sort.}

\item{sort_by}{Character vector of sort keys. Each entry is a column
name, optionally followed by \code{ASC} or \code{DESC} and by \verb{NULLS FIRST} or
\verb{NULLS LAST}, e.g. \code{"event_time DESC"} or \code{"id ASC NULLS LAST"}.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Declares how a table's data files should be sorted. DuckLake sorts data
on insert (unless the \code{sort_on_insert} option is disabled), during
compaction with \code{\link[=merge_adjacent_files]{merge_adjacent_files()}}, and when flushing inlined data
with \code{\link[=flush_inlined_data]{flush_inlined_data()}}. Sorted files carry tighter min/max
statistics, so filters on the sort columns prune files instead of
scanning them -- the complement to \code{\link[=set_table_partitioning]{set_table_partitioning()}} for
high-cardinality columns.
}
\details{
Runs \verb{ALTER TABLE ... SET SORTED BY (...)}. Only newly written files are
sorted; existing files keep their layout until compaction rewrites them.

DuckLake also accepts arbitrary SQL expressions as sort keys; this
wrapper deliberately accepts only column-based keys so the input can be
validated. For expression keys, run the \verb{ALTER TABLE} statement directly
with \code{\link[DBI:dbExecute]{DBI::dbExecute()}}.

To keep insert speed and sort the files only at compaction time, disable
sorting on insert with
\code{set_ducklake_option("sort_on_insert", FALSE, table_name = ...)}.
}
\examples{
\dontshow{if (ducklake_extension_available()) withAutoprint(\{ # examplesIf}
lake_dir <- tempfile("sort_lake_")
dir.create(lake_dir)
attach_ducklake("sort_lake", lake_path = lake_dir)
create_table(mtcars, "cars")

# Order rows so range filters can prune files
set_table_sorting("cars", "mpg")

# Compound key with explicit directions
set_table_sorting("cars", c("cyl ASC", "mpg DESC"))

detach_ducklake("sort_lake", shutdown = TRUE)
unlink(lake_dir, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=reset_table_sorting]{reset_table_sorting()}}, \code{\link[=set_ducklake_option]{set_ducklake_option()}},
\code{\link[=set_table_partitioning]{set_table_partitioning()}}

Other sorting:
\code{\link[=reset_table_sorting]{reset_table_sorting()}}
}
\concept{sorting}
