# Extract column assignments from SQL SELECT statement

Extract column assignments from SQL SELECT statement

## Usage

``` r
extract_assignments_from_sql(sql_text)
```

## Arguments

- sql_text:

  A SQL SELECT statement

## Value

A string of comma-separated column assignments for UPDATE SET clause
