# Get a DuckLake table

Get a DuckLake table

## Usage

``` r
get_ducklake_table(tbl_name)
```

## Arguments

- tbl_name:

  Character string, name of the table to retrieve

## Value

A DuckLake table of class `tbl_duckdb_connection` with the table name
stored as an attribute
